/*#include<stdio.h>
void printarray(int arr[],int size)
{
	int i;
	for( i=0;i<size;i++)
	{
		printf("%d",arr[i]);
	}
printf("\n");
}
int main()
{  
    int i;
	int numbers[] = {1,2,3,4,5};
	int size=sizeof(numbers)/sizeof(numbers[0]);
	printarray(numbers,size);
	printf("original numbers:\n");
	for(i=0;i<size;i++)
	{
		numbers[i]*=2;
	}
	printf("modified number :\n");
	printarray(numbers,size);
}*/


#include<stdio.h>
/*void printarray(int arr[],int size)
{
	int i;
	for( i=0;i<size;i++)
	{
		printf("%d",arr[i]);
	}
printf("\n");
}
int main()
{  
    int i;
	float sum;
	float numbers[] = {10,12,13,14,15};
	int size=sizeof(numbers)/sizeof(numbers[0]);
	printarray(numbers,size);
	printf("original numbers:\n");
	for(i=0;i<size;i++)
	{
		sum=+numbers[i];
		
	}
	printf("modified number :\n");
	int average;
	average=sum/size;
	printarray(numbers,size);
}*/
int main(){

float x=2.56;
int y ;
  y=x;
printf("y is %d",y);
}
